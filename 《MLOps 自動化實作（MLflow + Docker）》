🧩 實作範例：MLflow Tracking
import mlflow
import mlflow.sklearn
from sklearn.ensemble import RandomForestRegressor
from sklearn.metrics import mean_squared_error

# 訓練範例
model = RandomForestRegressor(n_estimators=100, max_depth=5)
model.fit(X_train, y_train)
pred = model.predict(X_test)

# 紀錄參數與結果
with mlflow.start_run():
    mlflow.log_param("n_estimators", 100)
    mlflow.log_param("max_depth", 5)
    mlflow.log_metric("rmse", mean_squared_error(y_test, pred, squared=False))
    mlflow.sklearn.log_model(model, "model")

四、🐳 Docker 封裝部署流程
1️⃣ 建立 Dockerfile
FROM python:3.10
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["mlflow", "models", "serve", "-m", "models:/my_model/Production", "-p", "5000"]

2️⃣ 建立映像檔
docker build -t mlflow-model .
3️⃣ 啟動服務
docker run -p 5000:5000 mlflow-model
