LeetCode 206 Reversed Linked List
Given the head of a singly linked list, reverse the list, and return the reversed list.
1->2->3->4->5

###coding answer Runtime: 0 ms(迭代法)###

class ListNode:
  def __init__(self, val=0,next=None):
         self.val=val
         self.next=next
def reverseList(head):
#1.初始化
   prev=None #虛擬節點(sentinel)
   curr=head #當前待反轉的節點
#2.遍歷時翻轉箭頭
   while=curr:
     next_node=curr.next #記住下一個位置(的地址) #用next_node保存curr.next
     curr.next=prev #反轉指針
     prev=curr #移動prev指針(prev前進)
     curr=next #移動curr指針(curr前進)
#3.返回新頭
return prev #新頭節點(新頭結點就是prev)

#4.測試
node3=ListNode(9)
node2=ListNode(8, node3)
node1=ListNode(7, node2)
head= node1
new_head=reverseList(head)
#打印結果:9->8->7->None
while new_head:
  print(new_head.val, end="->")
  new_head = new_head.next
print("None")


reference video from youtube>>>https://youtu.be/bIuVgt-emS8?feature=shared

###遞歸法###
class Solution:
    def reverseList(self, head):
#1. base case
        if not head or not head.next:
            #空鏈表或只剩一個節點
            return head
#2.遞歸關係
        new_head =self.reverseList(head.next) #相信遞歸能反轉後面的! #先反轉後面的部分
        head.next.next = head
        #讓下一個節點指向自己
        head.next = None
        #斷開原箭頭
        return new_head #傳遞新頭
