###暴力解法:遍歷###
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hashmap = {} #哈希表即字典
        for i in range(len(nums)): 
            for j in range (i+1, len(nums)):
                if nums[i] + nums[j] == target:
                    return [i, j]


###哈希表優化///runtime:3ms###
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hashmap = {} #哈希表即字典
        for i , num in enumerate(nums): 
            complement = target - num
            if complement in hashmap: #如果另一半在字典裡
                return [hashmap[complement], i] #直接返回答案
            hashmap[num] = i #否則把當前數字存入字典
